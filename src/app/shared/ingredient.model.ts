export class Ingredient{
    // public under constructor automatically declared variable and assigned the argument to that variable like:
    //public name !:string;
    //constructor(name:string){this.name=name}
constructor(public name:string,public amount:number){}
}